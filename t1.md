## Введение для новичков — знакомство с Git

Цель: дать понятное объяснение основных команд `git log` и смежных инструментов, научить запускать их в терминале и собирать отчёт о своей работе за последнюю неделю.

Время на выполнение: 7 часов.
- 0:30 — установка Git и проверка репозитория
- 2:00 — теория и практика базовых команд
- 2:00 — выполнение первых кейсов (Блоки 1–3)
- 1:30 — выполнение сложных кейсов (Блоки 4–5)
- 1:00 — подготовка отчётов и презентация результатов

Все инструкции далее адресованы студенту: выполняйте шаги и сохраняйте выводы в указанные файлы (`report_*.md`).

Где * ФамилияИмя на англ. языке.

---

## Теория: команды для работы с историей (пошагово, для новичков)

Инструкция: для каждой команды — прочитайте описание, посмотрите пример и выполните «Повтори сам» в своём локальном репозитории. Результаты вставьте в соответствующий `report_*.md`.

1) `git log`
  - Что делает: показывает полную историю коммитов текущей ветки — хэш, автора, дату и сообщение, а также тело сообщения и метаданные.
  - Комментарий: эта команда выводит всю доступную информацию по каждому коммиту; если коммитов нет — вы увидите сообщение об их отсутствии.
  - Пример использования:
```cmd
git log
```
  - Как читать: каждый блок — один коммит; самый верхний — последний коммит.
  - Повтори сам: выполните `git log` и вставьте первые 8–10 строк вывода в `report_basic_git.md`.

---

Если при выполнении `git log` вы увидите сообщение:

```
fatal: your current branch 'master' does not have any commits yet
```

не пугайтесь — это означает, что в репозитории ещё нет ни одного коммита. Такое случается, если репозиторий был только что создан (`git init`) или вы находитесь в пустой ветке после создания репозитория. Алгоритм действий (Windows `cmd`) для исправления:


1) Откройте `cmd` (Пуск → введите `cmd` → Enter). Если у вас ещё нет папки для репозитория, создайте её и перейдите в неё.

Комментарий: в Windows удобно создавать папку на рабочем столе или в `Documents`. Если путь содержит пробелы — используйте кавычки. Команда `cd /d` переключает диск и директорию.

Пример (замените `myrepo` на своё имя папки):

Создаём папку на рабочем столе:
Пояснение: `mkdir <path>` — создаёт указанную директорию; используйте кавычки если в пути есть пробелы.
```cmd
mkdir "%USERPROFILE%\Desktop\git-practice\myrepo"
```
Переходим в созданную папку:
Пояснение: `cd /d <path>` — переходит в директорию и переключает диск при необходимости (важно в Windows).
```cmd
cd /d "%USERPROFILE%\Desktop\git-practice\myrepo"
```

Если папка уже существует — выполните только `cd /d "путь_к_папке"`, например:

```cmd
cd /d "%USERPROFILE%\Desktop\git-practice\myrepo"
```

2) (Опционально) Если репозиторий ещё не инициализирован — запустите:

Пояснение: `git init` инициализирует новый пустой git-репозиторий в текущей папке.
```cmd
C:\path\to\your\repo>git init
```

3) Убедитесь, что Git знает ваше имя и email (если не настроено, коммит не удастся без ввода этих данных):

Пояснение: `git config --global user.name "..."` и `git config --global user.email "..."` — задают имя и email для ваших коммитов; флаг `--global` сохраняет настройки для всех репозиториев текущего пользователя.
```cmd
C:\path\to\your\repo>git config --global user.name "Ваше Имя"
C:\path\to\your\repo>git config --global user.email "you@example.com"
```

4) Создайте файл и сделайте первый коммит (в `cmd`):

Пояснение: первая команда создаёт файл `README.md` с содержимым `Initial README`; вторая — добавляет файл в индекс (`git add`), третья — сохраняет изменения в репозитории (коммит).
Пояснение к `git commit -m`: `-m "сообщение"` указывает текст сообщения прямо в командной строке, без открытия редактора.
```cmd
C:\path\to\your\repo>echo Initial README > README.md
C:\path\to\your\repo>git add README.md
C:\path\to\your\repo>git commit -m "Initial commit"
```

5) Проверьте историю снова:

Пояснение: `git log --oneline` выводит краткую историю коммитов (сокращённый хэш и сообщение) — удобно для проверки списка коммитов.
```cmd
C:\path\to\your\repo>git log --oneline
```

Теперь вы увидите хотя бы один коммит (Initial commit). Чтобы показать развитие истории, выполните ещё одно изменение и коммит:

Пояснение: `>>` добавляет строку в конец файла; затем снова индексируем и коммитим изменения.
```cmd
C:\path\to\your\repo>echo "Дополнение" >> README.md
C:\path\to\your\repo>git add README.md
C:\path\to\your\repo>git commit -m "Update README"
C:\path\to\your\repo>git log --oneline
```

В результате `git log --oneline` покажет две строки — два ваших коммита. Прикрепите вывод `git log --oneline` в ваш отчёт (`report_basic_git.md`) как доказательство того, что вы успешно устранили ошибку и выполнили коммиты.

Совет: если при `git commit` Git просит заполнить редактор для сообщения, просто введите сообщение, сохраните файл и закройте редактор (в Windows это обычно Notepad или другой графический редактор). Чтобы избежать открытия редактора, используйте `-m "сообщение"`, как в примерах выше.


2) `git log --oneline`
  - Что делает: компактный вывод — одна строка на коммит (короткий хэш + тема сообщения).
  - Комментарий: удобно для быстрого обзора истории и поиска последних изменений.
  - Пример:
```cmd
git log --oneline
```
  - Повтори сам: покажите 5 последних коммитов и запишите их в `report_basic_git.md`, сгруппировав по теме (настройка/фича/фикс).

3) `git log --pretty=format:"..."`
  - Что делает: настраиваемый формат вывода (можно указать, какие поля показывать).
  - Комментарий: полезно для экспорта в текст или парсинга скриптом — вы выбираете какие поля и в каком порядке.
  - Пример:
```cmd
git log --pretty=format:"%h | %an | %ad | %s" --date=iso -n 10
```
  - Повтори сам: экспортируйте 10 последних коммитов в файл `last10.txt` командой выше.

---

  ## 👤 Фильтрация по автору

  ### 7. Коммиты конкретного автора
  ```cmd
  git log --author="ИмяАвтора"
  git log --author="email@example.com"
  ```


  ## 🔍 Поиск в сообщениях

  ### 8. Поиск по сообщению коммита
  ```cmd
  git log --grep="текст для поиска"
  git log --grep="bugfix" -i  # -i для игнорирования регистра
  ```

  ---

  ## 📊 Визуализация ветвления

  ### 9. Графическое представление
  ```cmd
  git log --oneline --graph --all --decorate
  ```

  ### 10. Дерево коммитов
  ```cmd
  git log --oneline --graph --pretty=format:"%C(yellow)%h%Creset %C(blue)%an%Creset %C(green)%ad%Creset %s" --date=short
  ```

  ---

  ## 📁 Фильтрация по файлам

  ### 11. История изменений файла
  ```cmd
  git log -- filename.txt
  git log -p -- filename.txt  # с показом изменений
  ```
  ---

  ## 🎯 Полезные комбинации

  ### 12. Показать статистику
  ```cmd
  git log --stat
  ```

  Показывает измененные файлы и статистику.

  ### 13. Показать изменения
  ```cmd
  git log -p
  git log --patch
  ```

  Показывает разницу для каждого коммита.

  ```cmd
  git log --oneline --stat -n 5
  ```

  ---

  ## 💡 Практические примеры

  ### Для ежедневного использования:
  Последние 10 коммитов (компактно):
  ```cmd
  git log --oneline -n 10
  ```

  История с графикой веток:
  ```cmd
  git log --oneline --graph --all -n 15
  ```

  Мои коммиты за последнюю неделю:
  ```cmd
  git log --oneline --author="МоеИмя" --since="1 week ago"
  ```

  # Поиск коммита по сообщению
  ```cmd
  git log --oneline --grep="исправление" -i
  ```

  *Совет:* Для удобства можно создать алиасы в вашем shell-профиле (.bashrc, .zshrc):
  ```cmd
  alias gl='git log --oneline --graph --all'
  alias gla='git log --oneline --graph --all --author="ВашеИмя"'
  ```

  Выберите формат, который наиболее удобен для ваших задач!
  Отличная идея! Вот практические задания для студентов с использованием команд Git, от простых к сложным.

  ---

  **Практические задания по Git — от простых к сложным**

  **Цель:** Научиться анализировать историю репозитория, расследовать инциденты, готовить отчёты и работать с ветками при помощи команд Git.

  **Формат сдачи:** для каждого задания — текстовый отчёт (`.md`) с командами и их выводом, краткие выводы и при необходимости патч/предложение исправления.

  **Рекомендуемое время на пакет:** 60–120 минут (в зависимости от уровня).

  ---

  ## 📚 *Блок 1: Основное знакомство с историей*

  ### 1. Команда: git log
  *Задание: "Первое знакомство с проектом"*
  > Вы присоединились к команде разработки. Изучите историю коммитов проекта с помощью `git log`. Ответьте на вопросы:
  > 1. Сколько всего коммитов в проекте?
  > 2. Кто основной автор коммитов?
  > 3. Какая общая тематика сообщений коммитов (фичи, багфиксы, рефакторинг)?

  Пример команды:
```cmd
git log --pretty=format:"%h %an %ad %s" --date=short
```

  Результат: `report_1.md` — вывод команды и ответы на вопросы.

  ### 2. Команда: git log --oneline
  *Задание: "Анализ структуры коммитов"*
  > Используя `git log --oneline`, составьте хронологический список всех коммитов. Сгруппируйте их по темам (например: "Настройка проекта", "Добавление функционала", "Исправление ошибок"). Сделайте вывод о том, как велась разработка.

  Пояснение: компактный список коммитов с декорациями веток и тегов; полезно для обзора истории в одну строку.
```cmd
git log --oneline --decorate --all
```

  Результат: `report_2.md` с группировкой и примерами хэшей.

  ---

  ## 🔍 *Блок 2: Расследование и поиск*

  ### 3. Команда: git log --grep="bugfix" -i
  *Задание: "Охотник за багами"*
  > В проекте были критические исправления. Найдите все коммиты, связанные с исправлением ошибок, используя поиск по словам "bug", "fix", "error". Составьте отчет: какие модули проекта чаще всего требовали исправлений?

  Пояснение: поиск коммитов по ключевым словам; `-i` — игнорирует регистр; `--name-only` показывает только имена файлов, затронутых коммитом.
```cmd
git log --grep="bug\|fix\|error" -i --pretty=format:"%h %an %ad %s" --name-only
```

  Результат: `report_3.md` с перечислением коммитов и анализом модулей.

  ### 4. Команда: git log --author="Иван"
  *Задание: "Вклад разработчика"*
  > Проанализируйте вклад конкретного разработчика (Ивана) в проект. Сколько коммитов он сделал? Какой была его последняя работа? В какое время суток он обычно работает?

  Пояснение: фильтр по автору; выводит хэш, дату и сообщение; `--date=iso` форматирует дату в ISO.
```cmd
git log --author="Иван" --pretty=format:"%h %ad %s" --date=iso
```

  Результат: `report_4.md` — профиль разработчика (количество, последние коммиты, временная активность).

  ### 5. Команда: git log --since="2024-01-01" --until="2024-02-01"
  *Задание: "Отчет за месяц"*
  > Руководство запросило отчет о разработке за январь 2024 года. Предоставьте список всех коммитов за этот период с указанием авторов и тематики работ.

  Пояснение: фильтрация по дате; `--since` и `--until` задают интервал.
```cmd
git log --since="2024-01-01" --until="2024-02-01" --pretty=format:"%h %an %ad %s" --date=short
```

  Результат: `report_5.md` с перечислением коммитов за период и кратким анализом.

  ---

  ## 📊 *Блок 3: Визуальный анализ*

  ### 6. Команда: git log --oneline --graph --all
  *Задание: "Карта веток"*
  > Проект ведется по Git Flow. Визуализируйте дерево коммитов со всеми ветками. Ответьте:
  > 1. Сколько активных веток в проекте?
  > 2. Как часто происходили слияния feature-веток в develop?
  > 3. Были ли конфликты при слиянии (ищите "merge commit")?

  Пояснение: показывает компактную историю с графом ветвлений и только merge-коммиты (`--merges` показывает только слияния).
```cmd
git log --oneline --graph --decorate --all --merges
```

  Результат: `report_6.md` с визуальной картой и ответами.

  ### 7. Команда: git log --stat
  *Задание: "Анализ изменений"*
  > Изучите статистику изменений файлов. Определите:
  > - Какой файл изменялся чаще всего?
  > - В каких коммитах добавлялись новые файлы?
  > - Были ли коммиты, затрагивающие более 10 файлов одновременно?

  Пояснение: `--stat` показывает, какие файлы были изменены и сколько строк добавлено/удалено в каждом коммите.
```cmd
git log --stat --pretty=format:"%h %an %ad %s" --date=short
```

  Результат: `report_7.md` с подсчётами и примерами коммитов.

  ---

  ## 💻 *Блок 4: Практические кейсы*

  ### 8. Команда: git log -p -- main.py
  *Задание: "Эволюция алгоритма"*
  > Изучите историю изменений файла `main.py`. Проследите, как развивалась ключевая функция `calculate_score()`. Опишите, какие улучшения вносились в алгоритм и с какой целью.

  Пояснение: показываем историю и diffs только для `main.py`.
Пояснение (первая команда): форматированный список коммитов, касающихся `main.py`.
```cmd
git log --pretty=format:"%h %ad %an %s" --date=iso -- main.py
```
Пояснение (вторая команда): `-p` показывает патчи (diff) для каждого коммита по `main.py`.
```cmd
git log -p -- main.py
```

  Результат: `report_8.md` — хронология и анализ изменений.

  ### 9. Команда: git log --pretty=format:"..."
  *Задание: "Кастомный отчет"*
  > Настройте формат вывода git log для автоматического создания еженедельного отчета команде. В отчет должны входить: хэш, автор, дата, тема коммита. Экспортируйте 10 последних коммитов в текстовый файл.

  Пояснение: экспортирует 10 последних коммитов в файл `last10.txt` в формате: полный хэш|автор|дата|сообщение.
```cmd
git log -n 10 --pretty=format:"%H|%an|%ad|%s" --date=iso > last10.txt
```

  Результат: `last10.txt` и `report_9.md` с пояснениями.

  ### 10. Комбинация команд + git show
  *Задание: "Расследование инцидента"*
  > *Сценарий:* После коммита a1b2c3d перестала работать регистрация пользователей. Ваша задача:
  > 1. Найдите этот коммит в истории
  > 2. Изучите, какие именно изменения были сделаны (`git show a1b2c3d`)
  > 3. Найдите, кто автор и связаться с ним для консультации
  > 4. Предложите вариант исправления

  Результат: `incident_10.md` с выводом `git show`, анализом и предложением исправления.

  ---

  ## 🎯 *Блок 5: Командная работа*

  ### 11. Команда: git shortlog -s -n
  *Задание: "Рейтинг разработчиков"*
  > Составьте рейтинг участников команды по количеству коммитов. Проанализируйте: равномерно ли распределена нагрузка между разработчиками? Кто является тимлидом по факту?

  Пояснение: суммарный рейтинг авторов по числу коммитов; `-s` показывает только количество, `-n` сортирует по убыванию.
```cmd
git shortlog -s -n --all
```

  Результат: `report_11.md` с таблицей и выводами.

  ### 12. Комбинация: git log + анализ
  *Задание: "Аудит кода"*
  > Проведите полный аудит проекта:
  > 1. Найдите коммиты с слишком короткими сообщениями (< 10 символов)
  > 2. Выявите "массовые" коммиты, где изменялось > 15 файлов
  > 3. Найдите коммиты, сделанные в нерабочее время (после 20:00 или до 09:00)
  > 4. Составьте рекомендации по улучшению практик коммитинга

  Подход/команды (примеры):
  - короткие сообщения:
Пояснение: команда ниже выводит коммиты и отбирает строки, где длина сообщения меньше 10 символов (используется `awk`).
```cmd
git log --pretty=format:"%h|%ad|%s" --date=iso | awk -F'|' 'length($3)<10{print $0}'
```
  - массовые коммиты: парсинг
Пояснение: `git show --name-only <hash>` показывает список файлов в коммите — используйте для подсчёта числа файлов.
```cmd
git show --name-only <hash>
```
  для подсчёта числа файлов
  - время коммита:
Пояснение: выводим дату и время коммитов, затем фильтруем по часу в вашей предпочитаемой утилите (awk/PowerShell).
```cmd
git log --pretty=format:"%h %ad %an %s" --date=iso
```
  и фильтрация по часу

  Результат: `audit_12.md` с найденными примерами и рекомендациями.

  ---

  ## 📋 *Критерии оценки для преподавателя:*

  - *Правильность выполнения* - корректное использование флагов команд
  - *Глубина анализа* - способность делать выводы из полученных данных
  - *Оформление отчета* - четкость и структурированность ответов
  - *Работоспособность* - все команды выполняются без ошибок
  - *Интерпретация* - понимание контекста и причин изменений

  Каждое задание развивает не только технические навыки работы с Git, но и аналитическое мышление, столь важное для будущих разработчиков! 🚀


### Быстрый пример: создать README и первый коммит (с пояснениями)

Пояснение: создаёт файл `README.md` с содержимым `Initial README`.

Пояснение: `git add README.md` добавляет файл в индекс (staging area), готовя его к коммиту.

Пояснение: `git commit -m "Initial commit"` сохраняет индекс как новый коммит; флаг `-m` указывает сообщение прямо в командной строке.

```cmd
C:\path\to\your\repo>echo Initial README > README.md
C:\path\to\your\repo>git add README.md
C:\path\to\your\repo>git commit -m "Initial commit"
```

